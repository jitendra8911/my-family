import React from "react";
interface IProps {
    personName: string,
    imageName: string,
    descriptionTitle: string
}

function Moment(props: IProps) {
    return (
        <div>

        </div>
    )
}